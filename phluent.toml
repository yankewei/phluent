# Phluent configuration (TOML)
# Paths are resolved relative to this file's directory.

[sources.laravel]
type = "file"
dir = "data"
max_bytes = 10485760

[sinks.laravel]
type = "file"
dir = "output"
prefix = "laravel"
format = "ndjson"
compression = "gzip"
inputs = ["laravel"]
